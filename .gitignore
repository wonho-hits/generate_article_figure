# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg

# Virtual envs
.venv/
venv/
env/

# Env / secrets — NEVER commit .env
.env
.env.*
!.env.example

# Test / build caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Type-checker / formatter caches
.dmypy.json
dmypy.json

# Gradio cache (analytics, flagged samples)
.gradio/
gradio_cached_examples/
flagged/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude Code local settings (not shared); keep CLAUDE.md (project context)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Project artifacts (generated figures, downloads, etc.)
artifacts/
*.tmp

# uv: keep uv.lock committed (deterministic installs); ignore download cache
.uv-cache/
